name: codeql

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main
  schedule:
    - cron: '15 8 * * *'

jobs:
  shared:
    uses: amezin/js-actions-common/.github/workflows/shared-codeql.yml@60415ed634a78024d3cc1eeda0bf6b91ca4285b7 # v5.1.1
    permissions:
      contents: read
      security-events: write
